---
import Layout from '../layouts/Layout.astro';
import siteData from '../data/site.json';
---

<Layout title="Kontakt — Mr. Nice Vape Deutschland | Vape-Shop Köln" description="Kontaktieren Sie Mr. Nice Vape Deutschland: Produktberatung, Fragen zur Bestellung und Support per E-Mail oder Telefon aus Köln.">
  <Fragment slot="head">
    <meta name="keywords" content="mrnicevape kontakt, vape shop köln, mrnicevape telefon, mrnicevape email, vape beratung deutschland" />
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "ContactPage",
      "name": "Kontakt Mr. Nice Vape Deutschland",
      "description": "Kontaktieren Sie Mr. Nice Vape Deutschland per E-Mail oder Telefon.",
      "url": "https://mrnicevape.com/contact",
      "isPartOf": { "@type": "WebSite", "@id": "https://mrnicevape.com/#website" },
      "about": {
        "@type": "Store",
        "name": "Mr. Nice Vape Deutschland",
        "url": "https://mrnicevape.com",
        "logo": "https://mrnicevape.com/images/logo.svg",
        "address": {
          "@type": "PostalAddress",
          "streetAddress": "Musterstraße 12",
          "addressLocality": "Köln",
          "addressRegion": "NRW",
          "postalCode": "50667",
          "addressCountry": "DE"
        },
        "telephone": "+492211234567",
        "email": "info@mrnicevape.com",
        "openingHoursSpecification": [
          { "@type": "OpeningHoursSpecification", "dayOfWeek": ["Monday","Tuesday","Wednesday","Thursday","Friday"], "opens": "10:30", "closes": "19:30" },
          { "@type": "OpeningHoursSpecification", "dayOfWeek": "Saturday", "opens": "10:30", "closes": "18:00" },
          { "@type": "OpeningHoursSpecification", "dayOfWeek": "Sunday", "opens": "10:30", "closes": "16:00" }
        ]
      },
      "breadcrumb": {
        "@type": "BreadcrumbList",
        "itemListElement": [
          { "@type": "ListItem", "position": 1, "name": "Startseite", "item": "https://mrnicevape.com/" },
          { "@type": "ListItem", "position": 2, "name": "Kontakt", "item": "https://mrnicevape.com/contact" }
        ]
      }
    })} />
  </Fragment>

  <!-- Breadcrumb -->
  <nav class="breadcrumb-bar" aria-label="Breadcrumb">
    <div class="container">
      <ol class="breadcrumb">
        <li><a href="/">Startseite</a></li>
        <li><svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg></li>
        <li><span class="breadcrumb-current">Kontakt</span></li>
      </ol>
    </div>
  </nav>

  <!-- Hero -->
  <section class="contact-hero">
    <div class="contact-hero-pattern"></div>
    <div class="container contact-hero-inner">
      <div class="contact-hero-badge">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
        Antwort in der Regel innerhalb von 24 Stunden
      </div>
      <h1 class="font-display contact-hero-title">Wir helfen <span class="contact-hero-accent">gern weiter</span></h1>
      <p class="contact-hero-desc">Fragen zu Produkten, Versand oder Ihrer Bestellung? Unser Team antwortet persönlich, verständlich und ohne Umwege.</p>
    </div>
  </section>

  <!-- Contact Methods -->
  <section class="contact-methods">
    <div class="container">
      <div class="contact-grid">
        <!-- Visit Us -->
        <div class="contact-card">
          <div class="contact-card-accent visit"></div>
          <div class="contact-card-body">
            <div class="contact-card-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/><circle cx="12" cy="10" r="3"/></svg>
            </div>
            <h2 class="font-display contact-card-title">Standort in Köln</h2>
            <p class="contact-card-text">Persönliche Beratung, klare Empfehlungen und Support für Bestellungen in Deutschland.</p>
            <div class="contact-card-info">
              <strong>Musterstraße 12</strong><br />
              50667 Köln, NRW<br />
              Deutschland
            </div>
            <a href="https://maps.google.com/?q=Musterstra%C3%9Fe+12+50667+K%C3%B6ln+Deutschland" target="_blank" rel="noopener noreferrer" class="contact-card-action">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><path d="M15 3h6v6"/><path d="M10 14 21 3"/></svg>
              Route anzeigen
            </a>
          </div>
        </div>

        <!-- Email -->
        <div class="contact-card">
          <div class="contact-card-accent email"></div>
          <div class="contact-card-body">
            <div class="contact-card-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
            </div>
            <h2 class="font-display contact-card-title">E-Mail</h2>
            <p class="contact-card-text">Bestellungen, Produkte, Rückgaben — wir antworten an Werktagen meist innerhalb von 24 Stunden.</p>
            <div class="contact-card-info">
              <a href="mailto:info@mrnicevape.com" class="contact-email-link">info@mrnicevape.com</a>
            </div>
            <a href="mailto:info@mrnicevape.com" class="contact-card-action">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
              E-Mail senden
            </a>
          </div>
        </div>

        <!-- Phone -->
        <div class="contact-card">
          <div class="contact-card-accent phone"></div>
          <div class="contact-card-body">
            <div class="contact-card-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
            </div>
            <h2 class="font-display contact-card-title">Telefon</h2>
            <p class="contact-card-text">Sie brauchen schnell eine Antwort? Rufen Sie uns während der Servicezeiten an.</p>
            <div class="contact-card-info">
              <a href="tel:+492211234567" class="contact-email-link">+49 221 1234567</a>
            </div>
            <a href="tel:+492211234567" class="contact-card-action">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
              Jetzt anrufen
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Hours + Map Row -->
  <section class="contact-info-section">
    <div class="container">
      <div class="contact-info-grid">
        <!-- Hours -->
        <div class="contact-hours-card">
          <div class="contact-hours-header">
            <div class="contact-hours-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
            </div>
            <div>
              <h2 class="font-display contact-hours-title">Servicezeiten</h2>
              <p class="contact-hours-subtitle">Unser Team in Köln</p>
            </div>
          </div>
          <div class="contact-hours-table">
            <div class="contact-hours-row"><span>Montag – Freitag</span><span class="contact-hours-time">10:30 – 19:30 Uhr</span></div>
            <div class="contact-hours-row"><span>Samstag</span><span class="contact-hours-time">10:30 – 18:00 Uhr</span></div>
            <div class="contact-hours-row"><span>Sonntag</span><span class="contact-hours-time">10:30 – 16:00 Uhr</span></div>
            <div class="contact-hours-row"><span>Feiertage</span><span class="contact-hours-time closed">Geschlossen</span></div>
          </div>
          <p class="contact-hours-note">
            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></svg>
            Der Online-Shop ist rund um die Uhr geöffnet.
          </p>
        </div>

        <!-- Map -->
        <div class="contact-map-card">
          <h2 class="font-display contact-map-title">Standort</h2>
          <div class="contact-map-wrap">
            <iframe
              src="https://www.google.com/maps?q=K%C3%B6ln%2C%20Deutschland&output=embed"
              width="100%"
              height="280"
              style="border:0;border-radius:var(--radius-lg);"
              allowfullscreen=""
              loading="lazy"
              referrerpolicy="no-referrer-when-downgrade"
              title="Mr. Nice Vape Standort in Köln"
            ></iframe>
          </div>
          <div class="contact-map-address">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="var(--color-accent)" stroke-width="2"><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/><circle cx="12" cy="10" r="3"/></svg>
            <span>Musterstraße 12, 50667 Köln</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Form -->
  <section class="contact-form-section">
    <div class="container">
      <div class="contact-form-wrapper">
        <div class="contact-form-header">
          <span class="contact-section-label">Kontakt aufnehmen</span>
          <h2 class="font-display contact-form-title">Nachricht senden</h2>
          <p class="contact-form-desc">Füllen Sie das Formular aus. Wir melden uns so schnell wie möglich zurück.</p>
        </div>
        <form id="contact-form" class="contact-form" novalidate>
          <div class="contact-form-row">
            <div class="contact-form-group">
              <label for="contact-name" class="contact-label">Name <span class="required">*</span></label>
              <input type="text" id="contact-name" name="name" placeholder="Ihr Name" required class="contact-input" />
            </div>
            <div class="contact-form-group">
              <label for="contact-email" class="contact-label">E-Mail <span class="required">*</span></label>
              <input type="email" id="contact-email" name="email" placeholder="name@beispiel.de" required class="contact-input" />
            </div>
          </div>
          <div class="contact-form-group">
            <label for="contact-subject" class="contact-label">Betreff</label>
            <select id="contact-subject" name="subject" class="contact-input">
              <option value="">Thema auswählen...</option>
              <option value="Bestellanfrage">Bestellanfrage</option>
              <option value="Produktfrage">Produktfrage</option>
              <option value="Rückgabe und Erstattung">Rückgabe und Erstattung</option>
              <option value="Versand">Versand</option>
              <option value="Allgemein">Allgemein</option>
            </select>
          </div>
          <div class="contact-form-group">
            <label for="contact-message" class="contact-label">Nachricht <span class="required">*</span></label>
            <textarea id="contact-message" name="message" rows="5" placeholder="Wie können wir helfen?" required class="contact-input contact-textarea"></textarea>
          </div>
          <div class="contact-form-footer">
            <button type="submit" class="btn-primary contact-submit-btn" id="contact-submit">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m22 2-7 20-4-9-9-4z"/><path d="m22 2-10 10"/></svg>
              Nachricht senden
            </button>
            <span class="contact-form-hint">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
              Ihre Daten werden vertraulich behandelt.
            </span>
          </div>
          <div id="contact-success" class="contact-feedback success" style="display:none;">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><path d="m9 11 3 3L22 4"/></svg>
            <span>Vielen Dank für Ihre Nachricht. Wir melden uns innerhalb von 24 Stunden zurück.</span>
          </div>
          <div id="contact-error" class="contact-feedback error" style="display:none;">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 8v4"/><path d="M12 16h.01"/></svg>
            <span>Etwas ist schiefgelaufen. Bitte schreiben Sie uns direkt an info@mrnicevape.com.</span>
          </div>
        </form>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="contact-cta-section">
    <div class="container">
      <div class="contact-cta-box">
        <div class="contact-cta-icon">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
        </div>
        <h2 class="font-display contact-cta-title">Suchen Sie etwas Bestimmtes?</h2>
        <p class="contact-cta-desc">Stöbern Sie im gesamten Sortiment oder finden Sie schnelle Antworten in unseren FAQ.</p>
        <div class="contact-cta-actions">
          <a href="/collections/shop" class="btn-primary contact-cta-btn">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>
            Jetzt einkaufen
          </a>
          <a href="/faq" class="btn-secondary contact-cta-btn">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><path d="M12 17h.01"/></svg>
            FAQ ansehen
          </a>
        </div>
      </div>
    </div>
  </section>

  <script>
    const form = document.getElementById('contact-form') as HTMLFormElement;
    const submitBtn = document.getElementById('contact-submit') as HTMLButtonElement;
    const successEl = document.getElementById('contact-success')!;
    const errorEl = document.getElementById('contact-error')!;

    form?.addEventListener('submit', async (e) => {
      e.preventDefault();
      successEl.style.display = 'none';
      errorEl.style.display = 'none';

      const name = (document.getElementById('contact-name') as HTMLInputElement).value.trim();
      const email = (document.getElementById('contact-email') as HTMLInputElement).value.trim();
      const subject = (document.getElementById('contact-subject') as HTMLSelectElement).value;
      const message = (document.getElementById('contact-message') as HTMLTextAreaElement).value.trim();

      if (!name || !email || !message) {
        errorEl.querySelector('span')!.textContent = 'Bitte füllen Sie alle Pflichtfelder aus.';
        errorEl.style.display = 'flex';
        return;
      }

      submitBtn.disabled = true;
      submitBtn.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="spin"><circle cx="12" cy="12" r="10"/></svg> Wird gesendet...';

      try {
        const res = await fetch('/api/send-email', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            type: 'contact',
            data: { name, email, subject: subject || 'Allgemeine Anfrage', message }
          })
        });

        if (!res.ok) {
          const errData = await res.json().catch(() => ({}));
          throw new Error(errData.error || 'Nachricht konnte nicht gesendet werden');
        }

        successEl.style.display = 'flex';
        form.reset();
      } catch (err: any) {
        errorEl.querySelector('span')!.textContent = err.message || 'Etwas ist schiefgelaufen. Bitte versuchen Sie es erneut oder schreiben Sie direkt an info@mrnicevape.com';
        errorEl.style.display = 'flex';
      } finally {
        submitBtn.disabled = false;
        submitBtn.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m22 2-7 20-4-9-9-4z"/><path d="m22 2-10 10"/></svg> Nachricht senden';
      }
    });
  </script>
</Layout>

<style is:global>
  .breadcrumb-bar { background: var(--color-surface); border-bottom: 1px solid var(--color-border); padding: 0.85rem 0; }
  .breadcrumb { display: flex; align-items: center; gap: 0.45rem; flex-wrap: wrap; font-size: 0.8rem; list-style: none; }
  .breadcrumb a { color: var(--color-text-secondary); text-decoration: none; transition: color 0.15s ease; }
  .breadcrumb a:hover { color: var(--color-accent); }
  .breadcrumb svg { color: var(--color-text-dim); }
  .breadcrumb-current { color: var(--color-text); font-weight: 500; }

  /* ── Hero ── */
  .contact-hero {
    position: relative;
    padding: 5rem 0 4rem;
    text-align: center;
    background: linear-gradient(180deg, #F0FDF4 0%, #DCFCE7 40%, var(--color-bg) 100%);
    border-bottom: 1px solid var(--color-border);
    overflow: hidden;
  }
  [data-theme="dark"] .contact-hero {
    background: linear-gradient(180deg, rgba(22,163,74,0.08) 0%, rgba(22,163,74,0.03) 40%, var(--color-bg) 100%);
  }
  .contact-hero-pattern {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(circle at 1px 1px, rgba(22,163,74,0.07) 1px, transparent 0);
    background-size: 32px 32px;
    pointer-events: none;
  }
  .contact-hero-inner { position: relative; z-index: 1; }
  .contact-hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.45rem 1rem;
    background: rgba(22,163,74,0.1);
    border: 1px solid rgba(22,163,74,0.2);
    border-radius: 100px;
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--color-accent);
    margin-bottom: 1.25rem;
  }
  .contact-hero-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    color: var(--color-text);
    margin-bottom: 0.75rem;
    letter-spacing: -0.03em;
    line-height: 1.15;
  }
  .contact-hero-accent {
    background: linear-gradient(135deg, var(--color-accent), #059669);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .contact-hero-desc {
    color: var(--color-text-secondary);
    font-size: 1.05rem;
    max-width: 560px;
    margin: 0 auto;
    line-height: 1.7;
  }

  /* ── Contact Cards ── */
  .contact-methods { padding: 3rem 0 2rem; }
  .contact-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
  }
  @media (min-width: 768px) { .contact-grid { grid-template-columns: repeat(3, 1fr); } }

  .contact-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: border-color 0.25s ease, box-shadow 0.25s ease, transform 0.25s ease;
  }
  .contact-card:hover {
    border-color: rgba(22,163,74,0.3);
    box-shadow: 0 8px 32px rgba(22,163,74,0.08);
    transform: translateY(-3px);
  }
  .contact-card-accent {
    height: 3px;
    background: linear-gradient(90deg, var(--color-accent), #059669);
  }
  .contact-card-accent.visit { background: linear-gradient(90deg, #3b82f6, #2563eb); }
  .contact-card-accent.email { background: linear-gradient(90deg, var(--color-accent), #059669); }
  .contact-card-accent.phone { background: linear-gradient(90deg, #8b5cf6, #6d28d9); }
  .contact-card-body {
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    flex: 1;
  }
  .contact-card-icon {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #F0FDF4, #DCFCE7);
    border-radius: var(--radius-lg);
    color: var(--color-accent);
    margin-bottom: 1rem;
  }
  [data-theme="dark"] .contact-card-icon {
    background: rgba(22,163,74,0.12);
  }
  .contact-card-title {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: 0.5rem;
  }
  .contact-card-text {
    font-size: 0.85rem;
    color: var(--color-text-secondary);
    line-height: 1.65;
    margin-bottom: 1rem;
    flex: 1;
  }
  .contact-card-info {
    font-size: 0.88rem;
    color: var(--color-text);
    font-weight: 500;
    line-height: 1.55;
    margin-bottom: 1.25rem;
    padding: 0.85rem;
    background: var(--color-surface-alt);
    border-radius: var(--radius-md);
    border: 1px solid var(--color-border);
  }
  .contact-email-link {
    color: var(--color-accent);
    text-decoration: none;
    font-weight: 600;
    transition: color 0.15s;
  }
  .contact-email-link:hover { color: var(--color-accent-hover); text-decoration: underline; }
  .contact-card-action {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-display);
    font-size: 0.82rem;
    font-weight: 700;
    color: var(--color-accent);
    text-decoration: none;
    transition: gap 0.2s ease, color 0.15s;
  }
  .contact-card-action:hover { gap: 0.65rem; color: var(--color-accent-hover); }

  /* ── Hours + Map Grid ── */
  .contact-info-section { padding: 1rem 0 2.5rem; }
  .contact-info-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
    max-width: 800px;
    margin: 0 auto;
  }
  @media (min-width: 640px) {
    .contact-info-grid { grid-template-columns: 1fr 1fr; }
  }

  .contact-hours-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: 1.75rem;
  }
  .contact-hours-header {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    margin-bottom: 1.25rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--color-border);
  }
  .contact-hours-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #F0FDF4, #DCFCE7);
    border-radius: var(--radius-lg);
    color: var(--color-accent);
    flex-shrink: 0;
  }
  [data-theme="dark"] .contact-hours-icon {
    background: rgba(22,163,74,0.12);
  }
  .contact-hours-title {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--color-text);
  }
  .contact-hours-subtitle {
    font-size: 0.78rem;
    color: var(--color-text-muted);
    margin-top: 0.1rem;
  }
  .contact-hours-table { flex: 1; }
  .contact-hours-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.6rem 0;
    border-bottom: 1px solid var(--color-border);
    font-size: 0.85rem;
    color: var(--color-text);
  }
  .contact-hours-row:last-child { border-bottom: none; }
  .contact-hours-time { font-weight: 600; color: var(--color-text-secondary); }
  .contact-hours-time.closed { color: #ef4444; font-weight: 700; }
  .contact-hours-note {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.75rem;
    color: var(--color-text-muted);
    margin-top: 1rem;
    padding-top: 0.75rem;
    border-top: 1px solid var(--color-border);
  }

  .contact-map-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
  }
  .contact-map-title {
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: 1rem;
  }
  .contact-map-wrap {
    border-radius: var(--radius-lg);
    overflow: hidden;
    flex: 1;
    min-height: 200px;
  }
  .contact-map-address {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.78rem;
    color: var(--color-text-muted);
    margin-top: 0.85rem;
    padding-top: 0.75rem;
    border-top: 1px solid var(--color-border);
  }

  /* ── Contact Form ── */
  .contact-form-section { padding: 0 0 2.5rem; }
  .contact-section-label {
    display: inline-block;
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 0.35rem;
  }
  .contact-form-wrapper {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: 2.5rem;
    max-width: 700px;
    margin: 0 auto;
    box-shadow: 0 4px 24px rgba(0,0,0,0.03);
  }
  .contact-form-header {
    margin-bottom: 1.75rem;
  }
  .contact-form-title {
    font-size: 1.25rem;
    font-weight: 800;
    color: var(--color-text);
    letter-spacing: -0.02em;
  }
  .contact-form-desc {
    font-size: 0.88rem;
    color: var(--color-text-secondary);
    margin-top: 0.35rem;
    line-height: 1.6;
  }
  .contact-form { display: flex; flex-direction: column; gap: 1rem; }
  .contact-form-row {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }
  @media (min-width: 480px) { .contact-form-row { grid-template-columns: repeat(2, 1fr); } }
  .contact-form-group { display: flex; flex-direction: column; gap: 0.35rem; }
  .contact-label {
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--color-text);
  }
  .contact-label .required { color: #ef4444; }
  .contact-input {
    padding: 0.7rem 0.9rem;
    font-size: 0.88rem;
    color: var(--color-text);
    background: var(--color-bg);
    border: 1.5px solid var(--color-border);
    border-radius: var(--radius-md);
    transition: border-color 0.18s ease, box-shadow 0.18s ease;
    font-family: var(--font-body);
  }
  .contact-input:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(22, 163, 74, 0.12);
  }
  .contact-textarea { resize: vertical; min-height: 130px; }
  .contact-form-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .contact-submit-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 2rem;
    font-size: 0.88rem;
  }
  .contact-form-hint {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.72rem;
    color: var(--color-text-muted);
  }
  .contact-form-hint svg { color: var(--color-accent); }
  .contact-feedback {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.85rem 1rem;
    border-radius: var(--radius-md);
    font-size: 0.85rem;
    font-weight: 500;
  }
  .contact-feedback.success {
    background: #F0FDF4;
    color: #166534;
    border: 1px solid rgba(22, 163, 74, 0.25);
  }
  .contact-feedback.error {
    background: #FEF2F2;
    color: #991B1B;
    border: 1px solid rgba(239, 68, 68, 0.25);
  }
  .spin { animation: spin 1s linear infinite; }
  @keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

  /* ── CTA ── */
  .contact-cta-section { padding: 0 0 4.5rem; }
  .contact-cta-box {
    position: relative;
    background: linear-gradient(145deg, #F0FDF4 0%, #DCFCE7 100%);
    border: 1px solid rgba(22, 163, 74, 0.2);
    border-radius: var(--radius-xl);
    padding: 3.5rem 2rem;
    text-align: center;
    box-shadow: 0 8px 32px rgba(22, 163, 74, 0.08);
  }
  [data-theme="dark"] .contact-cta-box {
    background: linear-gradient(145deg, rgba(22,163,74,0.08), rgba(22,163,74,0.04));
  }
  .contact-cta-icon {
    width: 56px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(22,163,74,0.12);
    border-radius: 50%;
    color: var(--color-accent);
    margin: 0 auto 1.25rem;
  }
  .contact-cta-title {
    font-size: 1.35rem;
    font-weight: 800;
    color: var(--color-text);
    margin-bottom: 0.5rem;
    letter-spacing: -0.02em;
  }
  .contact-cta-desc {
    color: var(--color-text-secondary);
    font-size: 0.92rem;
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }
  .contact-cta-actions {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    justify-content: center;
  }
  .contact-cta-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.75rem;
  }
</style>
